var array = ["This", "is", "my", "array", "being", "printed."];

//using the for loop
//start;stop;increment
//bracket notation for i to iterate through each value
for(let i = 0; i < array.length; i++){
    console.log(array[i]);
}
